import React from 'react';
import GameState from "./engine/GameState";

function App() {
  console.log(GameState.startingPosition().toString());

  return (
    <div>
    </div>
  );
}

export default App;
